{
  "name": "quest-payments",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc --build packages/*/tsconfig.json",
    "clean": "tsc --build --clean packages/*/tsconfig.json",
    "test": "vitest run",
    "lint": "eslint packages/*/src"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^7.0.0",
    "@typescript-eslint/parser": "^7.0.0",
    "eslint": "^8.57.0",
    "typescript": "5.4",
    "vitest": "^1.6.0"
  }
}
